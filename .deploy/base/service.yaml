apiVersion: v1
kind: Service
metadata:
  name: neo-ticket-service
  labels:
    app.kubernetes.io/name: neo-ticket-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: neo-ticket-service
  ports:
    - name: http
      port: 80
      targetPort: http
